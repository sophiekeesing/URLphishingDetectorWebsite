{
  "name": "chick-check-website",
  "private": true,
  "type": "module",
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev:web": "npm run dev -w web",
    "dev:server": "npm run dev -w server",
    "build:web": "npm run build -w web",
    "build:server": "npm run build -w server"
  }
}
